<%= form_for(current_user.votes.build,
             remote: true) do |f| %>
  <div><%= hidden_field_tag :micropost_id, micropost_id %></div>
  <%= f.submit "Vote", class: "btn btn-primary" %>
<% end %>