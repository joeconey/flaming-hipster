<%= form_for(@question) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>

  <div class="field">
    <%= f.text_area :name, placeholder: "What is your question?" %>
  </div>
  <%= f.submit "Post", class: "btn btn-primary" %>
<% end %>
